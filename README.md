I build and operate production cloud platforms and AI systems: LLMs and agents wired into real business data and workflows.

With ~6 years as a software engineer, I take products from architecture to production and keep them running at scale — full-stack (Angular,
Next.js, React, NestJS, Python, FastAPI) on Azure and AWS, with applied AI inside real workflows, not demos.

Today I lead architecture, infrastructure and AI for BeatHub OS, the operations platform of Colombia's number one live entertainment group,
and I own deployments, reliability and capacity across its entire app portfolio. There I built Helios, the AI runtime (Python/FastAPI) that runs
an Azure AI Foundry agent with 12+ typed tools over the data warehouse, SSE streaming and end-to-end tracing.
LLMs in production — deploying and operating models (OpenAI, Anthropic, Meta) on Azure AI Foundry and AWS Bedrock, choosing the
model by cost, latency and quality.

Agents that do real work — tool calling over governed data, RAG (pgvector), web search, conversational context and multi-agent
orchestration with LangChain and LangGraph.

AI reliability — tracing, evaluators and hallucination control with Azure AI Foundry, Bedrock and LangSmith: if it can't be measured, it
doesn't ship.

Multi-cloud architecture — Azure (App Service, Functions, Virtual Machines, Azure SQL, Cosmos DB, Blob Storage, Data Lake, VNet, DNS,
Entra ID, Microsoft Graph) and AWS (EC2, Lambda, S3, RDS, DynamoDB, Bedrock, IAM), with CI/CD and security for high-traffic spikes.
End-to-end solutions — backend, frontend, data and integrations as a single system, from architecture to deployment.

I care about tangible outcomes: less manual work, faster decisions, and a platform a team can build on safely.
